plugin: amazon.aws.aws_ec2
regions:
- us-east-1

hostnames:
  - name: 'private-ip-address'
filters:
  # This selects only running instances with tag `Environment` tag set to `dev`.
  tag:Environment: frontend
  instance-state-name : running

